name: "Publish"
on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
      - "v*"

jobs:
  publish:
    uses: marcosfpr/ltrs/.github/template/publish.yml@master
  publish-alloy:
    uses: marcosfpr/ltrs/.github/template/publish.yml@master
    with:
      alloy: true
