name: "Publish"
on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
      - "v*"

jobs:
  publish:
    uses: ./.github/template/publish.yml
  publish-alloy:
    uses: ./.github/template/publish.yml
    with:
      alloy: true
